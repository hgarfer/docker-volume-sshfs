# AI Agent Instructions

This repository is a fork of `vieux/docker-volume-sshfs`, which is **archived and read-only**.

## Pull Requests

- **ALWAYS** target the fork: `hgarfer/docker-volume-sshfs`.
- **NEVER** try to create Pull Requests against the upstream repository (`vieux/docker-volume-sshfs`).
- When using the `gh` CLI tool, explicitly specify the repository argument if necessary, or ensure the context is set to the fork.

Example:
```bash
gh pr create --repo hgarfer/docker-volume-sshfs ...
```
