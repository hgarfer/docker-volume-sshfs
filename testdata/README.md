# Test Data

This directory contains test fixtures and sample data for the docker-volume-sshfs test suite.

## Directory Structure

- `state/` - Sample state files for testing driver state persistence
- `volumes/` - Mock volume mountpoint directories for testing

## Files

### state/sample-state.json
Sample state file with two test volumes, used for testing state loading and persistence.
